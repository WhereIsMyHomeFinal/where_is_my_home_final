<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.homeapp.model.dao.NoticeDAO">

	<select id="getNotices" resultMap="com.ssafy.resultmaps.NoticeResultMap">
		select id, title, userid, content 
		from notice 
	</select>

	<select id="getNotice" parameterType="int" resultType="Notice">
		select id, title, userid, content 
		from notice 
		where id = #{id} 
	</select>
	
	<insert id="registerNotice" parameterType="Notice">
		insert into notice (title, userid, content) 
		values (#{title}, #{userid}, #{content}) 
	</insert>
	
	<update id="modifyNotice" parameterType="Notice">
		update notice 
		set title = #{title}, content = #{content} 
		where id = #{id} 
	</update>
	
	<delete id="deleteNotice" parameterType="int">
		delete from notice 
		where id = #{id} 
	</delete>
	
</mapper>